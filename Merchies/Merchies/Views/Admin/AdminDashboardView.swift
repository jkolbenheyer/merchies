import SwiftUI
import Foundation

// Admin Views (Simplified placeholders)

struct AdminDashboardView: View {
    var body: some View {
        Text("Admin Dashboard")
            .font(.largeTitle)
    }
}
